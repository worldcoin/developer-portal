query CheckNullifierV4($nullifier: numeric!) {
  nullifier_v4(where: { nullifier: { _eq: $nullifier } }) {
    id
    nullifier
    action_v4_id
    created_at
    action_v4 {
      id
      rp_id
      action
      environment
    }
  }
}
