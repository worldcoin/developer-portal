/**
 * TODO: This stub is used to be able to run the tests. Due to the build nature of IDKit it's causing an issue with
 * imports. Jest not properly handling the ESM module. We should find a way of properly loading IDKit in Jest.
 */

const validateABILikeEncoding = (input: string) => {
  return true;
};

export const internal = {
  validateABILikeEncoding,
};
