import handleGraphQL from "pages/api/v1/graphql";

// FIXME
describe("/api/v1/graphql", () => {
  test("API key is handled", async () => {});
  test("request without API keys is proxied directly", async () => {});
  test("invalid API key throws error", async () => {});
  test("expired API key throws error", async () => {});
});
