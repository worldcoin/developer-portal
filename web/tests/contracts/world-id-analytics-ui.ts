/**
 * Provisional shared-component seam.
 *
 * Product behavior tests import this adapter rather than freezing the final
 * shared module location throughout the suite. [HITL] replaces this single
 * export after the UI approach is approved.
 */
export { WorldIdAnalyticsGraph } from "@/scenes/PortalV3/Teams/TeamId/Apps/AppId/WorldId/common/WorldIdAnalyticsGraph";
