query FetchUser($email: String, $auth0Id: String) {
  user(
    where: { _or: [{ email: { _eq: $email } }, { auth0Id: { _eq: $auth0Id } }] }
  ) {
    id
    name
    email
    auth0Id
    team_id
  }
}
