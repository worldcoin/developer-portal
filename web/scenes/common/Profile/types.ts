export { additionalColors as colors } from "@/lib/additional-colors";
export {
  getColorByName,
  getColorName,
  isColorName,
} from "@/lib/additional-colors";
export type { Color, ColorName } from "@/lib/additional-colors";
