query FetchMe($userId: String!) {
  user_by_pk(id: $userId) {
    id
    name
    email
    world_id_nullifier
    posthog_id

    memberships {
      role
      team {
        id
        name
      }
    }
  }
}
