query FetchApps {
  app {
    id
    app_metadata {
      name
    }
    verified_app_metadata: app_metadata(
      where: { verification_status: { _eq: "verified" } }
    ) {
      logo_img_url
    }
  }
}
