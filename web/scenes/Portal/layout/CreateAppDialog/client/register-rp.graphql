mutation RegisterRp($app_id: String!, $mode: String, $signer_address: String) {
  register_rp(app_id: $app_id, mode: $mode, signer_address: $signer_address) {
    rp_id
    manager_address
    signer_address
    status
    operation_hash
  }
}
