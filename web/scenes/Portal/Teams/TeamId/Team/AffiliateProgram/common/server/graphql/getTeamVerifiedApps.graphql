query GetTeamVerifiedApps($teamId: String!) {
  app(
    where: {
      team_id: { _eq: $teamId }
      app_metadata: { verification_status: { _eq: "verified" } }
      is_banned: { _eq: false }
    }
  ) {
    id
  }
}
