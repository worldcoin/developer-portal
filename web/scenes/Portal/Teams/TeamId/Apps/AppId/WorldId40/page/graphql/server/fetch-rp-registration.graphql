query FetchRpRegistration($appId: String!) {
  rp_registration(where: { app_id: { _eq: $appId } }) {
    rp_id
    app_id
    status
    staging_status
    mode
    signer_address
    created_at
    updated_at
  }
}
