query FetchAppEnv($id: String!) {
  app(where: { id: { _eq: $id } }) {
    id
    engine
    rp_registration {
      rp_id
    }
  }
  action(where: { app_id: { _eq: $id } }, limit: 1) {
    id
  }
}
