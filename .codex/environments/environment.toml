# THIS IS AUTOGENERATED. DO NOT EDIT MANUALLY
version = 1
name = "developer-portal"

[setup]
script = '''
set -eu

source_env="$CODEX_SOURCE_TREE_PATH/web/.env"
fallback_env="$CODEX_WORKTREE_PATH/web/.env.example"
worktree_env="$CODEX_WORKTREE_PATH/web/.env"

if [ -f "$source_env" ]; then
  (umask 077; cp "$source_env" "$worktree_env")
elif [ -f "$fallback_env" ]; then
  echo "No source web/.env found; copying web/.env.example"
  (umask 077; cp "$fallback_env" "$worktree_env")
else
  echo "Missing both source web/.env and worktree web/.env.example" >&2
  exit 1
fi

cd "$CODEX_WORKTREE_PATH/web"
pnpm install --frozen-lockfile
'''
