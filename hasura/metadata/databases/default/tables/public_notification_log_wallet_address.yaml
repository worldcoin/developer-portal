table:
  name: notification_log_wallet_address
  schema: public
insert_permissions:
  - role: service
    permission:
      check: {}
      columns:
        - notification_log_id
        - wallet_address
        - created_at
  - role: reviewer
    permission:
      check: {}
      columns:
        - notification_log_id
        - wallet_address
        - created_at
select_permissions:
  - role: service
    permission:
      columns:
        - id
        - notification_log_id
        - wallet_address
        - created_at
      filter: {}
  - role: reviewer
    permission:
      columns:
        - id
        - notification_log_id
        - wallet_address
        - created_at
      filter: {}
delete_permissions:
  - role: service
    permission:
      filter: {}
  - role: reviewer
    permission:
      filter: {}
