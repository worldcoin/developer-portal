table:
  name: nullifier_v4
  schema: public
object_relationships:
  - name: action_v4
    using:
      foreign_key_constraint_on: action_v4_id
insert_permissions:
  - role: service
    permission:
      check: {}
      columns:
        - action_v4_id
        - nullifier
        - uses
select_permissions:
  - role: service
    permission:
      columns:
        - id
        - created_at
        - updated_at
        - action_v4_id
        - nullifier
        - uses
      filter: {}
  - role: user
    permission:
      columns:
        - id
        - created_at
        - action_v4_id
        - nullifier
      filter:
        action_v4:
          rp_registration:
            app:
              _and:
                - deleted_at:
                    _is_null: true
                - team:
                    memberships:
                      user_id:
                        _eq: X-Hasura-User-Id
      allow_aggregations: true
update_permissions:
  - role: service
    permission:
      columns:
        - uses
      filter: {}
      check: {}
    comment: Only the atomic verify mutation increments uses.
