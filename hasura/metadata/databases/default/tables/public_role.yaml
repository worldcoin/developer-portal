table:
  name: role
  schema: public
is_enum: true
array_relationships:
  - name: memberships
    using:
      foreign_key_constraint_on:
        column: role
        table:
          name: membership
          schema: public
select_permissions:
  - role: user
    permission:
      columns:
        - value
      filter: {}
