table:
  name: sandbox_access_request
  schema: public
insert_permissions:
  - role: service
    permission:
      check: {}
      columns:
        - google_email
        - requested_by
        - team_id
        - status
        - processed_at
select_permissions:
  - role: service
    permission:
      columns:
        - id
        - google_email
        - requested_by
        - team_id
        - status
        - created_at
        - processed_at
      filter: {}
update_permissions:
  - role: service
    permission:
      columns:
        - status
        - processed_at
        - requested_by
      filter: {}
      check: {}
