table:
  schema: public
  name: action
object_relationships:
  - name: app
    using:
      foreign_key_constraint_on: app_id
array_relationships:
  - name: nullifiers
    using:
      foreign_key_constraint_on:
        column: action_id
        table:
          schema: public
          name: nullifier
select_permissions:
  - role: user
    permission:
      columns:
        - app_id
        - description
        - id
        - max_n
        - max_verifications
        - name
      filter:
        app:
          team_id:
            _eq: X-Hasura-Team-Id
