table:
  name: nullifier
  schema: public
select_permissions:
  - role: service
    permission:
      columns:
        - id
        - merkle_root
        - nullifier_hash
        - verification_level
      filter: {}
