table:
  name: api_key
  schema: public
select_permissions:
  - role: service
    permission:
      columns:
        - id
        - is_active
        - team_id
      filter:
        is_active:
          _eq: true
